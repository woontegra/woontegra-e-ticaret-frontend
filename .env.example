# Web Environment Variables
# Copy to .env and fill in values

# Lokal API:
# VITE_API_URL=http://localhost:3001

# Railway API (lokal frontend → uzak backend):
# Railway → API servisi → Settings → Networking → Public domain
VITE_API_URL=https://SENIN-RAILWAY-API-DOMAIN.up.railway.app
